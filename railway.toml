[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.dashboard"

[deploy]
startCommand = "npm run db:push && npm start -w packages/dashboard"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
